name: asesorias
description: Get to know Firebase with Flutter
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_slidable: ^0.6.0

  shared_preferences: ^2.0.5

  scoped_model: ^1.1.0


  jwt_decoder: ^1.0.3

  uuid: ^3.0.4
  google_fonts: ^2.0.0
  cloud_firestore: ^1.0.0
  firebase_auth: ^1.0.0
  provider: ^5.0.0
  table_calendar: ^3.0.0
  dart_jsonwebtoken: ^2.1.0
  date_field: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true
  assets:
    - assets/image/334588-128.png
    - assets/image/pizarraEntrenos.jpg
    - assets/image/pizarra_negra.jpg
